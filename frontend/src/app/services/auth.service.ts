import { Injectable } from '@angular/core';

// MEMORY.md: Auth + user management (roles/permissions).
@Injectable({ providedIn: 'root' })
export class AuthService {
  // TODO: login(), register(), logout(), currentUser(), token storage.
}
