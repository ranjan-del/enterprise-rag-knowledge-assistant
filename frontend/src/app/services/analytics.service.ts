import { Injectable } from '@angular/core';

// MEMORY.md: Dashboard analytics.
@Injectable({ providedIn: 'root' })
export class AnalyticsService {
  // TODO: overview(), usage() for dashboard tiles + charts.
}
