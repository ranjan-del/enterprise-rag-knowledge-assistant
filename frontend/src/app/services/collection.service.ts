import { Injectable } from '@angular/core';

// MEMORY.md: organize documents into collections.
@Injectable({ providedIn: 'root' })
export class CollectionService {
  // TODO: list(), create(), get(id), delete(id).
}
