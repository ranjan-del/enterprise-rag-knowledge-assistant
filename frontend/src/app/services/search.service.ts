import { Injectable } from '@angular/core';

// MEMORY.md: semantic/metadata/hybrid search + cited answers.
@Injectable({ providedIn: 'root' })
export class SearchService {
  // TODO: query(), semantic(), hybrid() returning answer + citations.
}
