import { Injectable } from '@angular/core';

// MEMORY.md: Multi-format upload + document management.
@Injectable({ providedIn: 'root' })
export class DocumentService {
  // TODO: list(), upload(file), get(id), delete(id).
}
