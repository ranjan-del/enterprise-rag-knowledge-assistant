import { Injectable } from '@angular/core';

// MEMORY.md: Admin (upload, delete, versioning, permissions).
@Injectable({ providedIn: 'root' })
export class AdminService {
  // TODO: listUsers(), setPermissions(), createVersion(), deleteDocument().
}
