import { Component } from '@angular/core';

// Documents page. MEMORY.md: Multi-format upload + parsers; document management.
// TODO: upload widget (PDF/DOCX/PPTX/TXT/CSV) + document list via DocumentService.
@Component({
  selector: 'app-documents',
  standalone: true,
  templateUrl: './documents.component.html',
})
export class DocumentsComponent {}
