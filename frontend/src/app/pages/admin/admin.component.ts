import { Component } from '@angular/core';

// Admin page. MEMORY.md: Admin (upload, delete, versioning, permissions).
// TODO: user management + document versioning/permissions via AdminService.
@Component({
  selector: 'app-admin',
  standalone: true,
  templateUrl: './admin.component.html',
})
export class AdminComponent {}
