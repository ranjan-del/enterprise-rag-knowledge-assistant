import { Component } from '@angular/core';

// Search page. MEMORY.md: semantic/metadata/hybrid search + cited answers.
// TODO: query box, result list, answer panel (confidence, citations, highlights, source).
@Component({
  selector: 'app-search',
  standalone: true,
  templateUrl: './search.component.html',
})
export class SearchComponent {}
