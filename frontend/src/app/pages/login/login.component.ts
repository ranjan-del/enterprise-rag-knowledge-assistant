import { Component } from '@angular/core';

// Login page. MEMORY.md: Auth + user management (roles/permissions).
// TODO: build reactive form + call AuthService.login().
@Component({
  selector: 'app-login',
  standalone: true,
  templateUrl: './login.component.html',
})
export class LoginComponent {}
