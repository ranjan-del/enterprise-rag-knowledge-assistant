import { Component } from '@angular/core';

// Analytics page. MEMORY.md: Dashboard analytics.
// TODO: usage charts (query volume, popular documents) via AnalyticsService.
@Component({
  selector: 'app-analytics',
  standalone: true,
  templateUrl: './analytics.component.html',
})
export class AnalyticsComponent {}
